<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Edit Data Produksi</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; max-width: 600px; margin: 40px auto; padding: 20px; background-color: #f4f7f6; color: #333; }
        .card { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        h1 { text-align: center; color: #2c3e50; margin-top: 0; }
        label { display: block; margin-top: 15px; font-weight: bold; color: #34495e; }
        input { width: 100%; padding: 10px; margin-top: 5px; border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; }
        .btn-group { margin-top: 25px; text-align: center; }
        .btn { padding: 10px 20px; color: white; text-decoration: none; border-radius: 6px; border: none; cursor: pointer; font-size: 1rem; }
        .btn-save { background-color: #27ae60; }
        .btn-cancel { background-color: #95a5a6; margin-left: 10px; }
    </style>
</head>
<body>
    <div class="card">
        <h1>✏️ Edit Data</h1>
        <p style="text-align: center; color: #7f8c8d;">Mengedit data untuk produk: <strong><%= log.Product.name %></strong></p>
        
        <form action="/update-log/<%= log.id %>" method="POST">
            
            <label>Tanggal Produksi:</label>
            <input type="date" name="date" value="<%= new Date(log.date).toISOString().split('T')[0] %>" required>

            <label>Total Diproduksi (Unit):</label>
            <input type="number" name="total_produced" value="<%= log.total_produced %>" required>

            <label>Jumlah Cacat (Unit):</label>
            <input type="number" name="defect_count" value="<%= log.defect_count %>" required>

            <div class="btn-group">
                <button type="submit" class="btn btn-save">💾 Simpan Perubahan</button>
                <a href="/" class="btn btn-cancel">Batal</a>
            </div>
        </form>
    </div>
</body>
</html>